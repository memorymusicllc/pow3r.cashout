/**
 * Theme Provider Component
 * Manages dark/light mode theme system with context and local storage
 * 
 * @version 1.0.0
 * @date 2025-10-12
 * @category Core
 * @tags theme, dark-mode, light-mode, context, provider
 */

import * as React from "react"

// Re-export the custom theme provider
export { ThemeProvider, useTheme } from '@/lib/theme-context'
